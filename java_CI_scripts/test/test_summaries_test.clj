(ns test-summaries-test
  (:require [clojure.test :refer [deftest is testing]])
  (:require [test-summaries :refer :all]))

; create folder, check if correct, then delete it!
(deftest create-test-sum-dir-test
  )
